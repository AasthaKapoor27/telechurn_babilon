services:
  - type: web
    name: telechurn-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    # MODEL_PATH defaults to backend/model.pkl (committed to repo)
    # Override here if you rename the file
